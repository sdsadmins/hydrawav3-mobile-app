// Moved to `lib/core/theme/hw_tokens.dart` when the handoff token set grew
// beyond the Devices feature (radii, spacing, type scale, motion). This shim
// keeps the existing `RefPalette` / `GoalColor` imports working.
export 'package:hydrawav3/core/theme/hw_tokens.dart' show RefPalette, GoalColor;
